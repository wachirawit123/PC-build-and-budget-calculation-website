# text — หน้าข้อความล้วน

**แสดง:** หัวข้อ + ย่อหน้า จาก list ใน `build()` (หรือจะเขียน HTML ตรง ๆ ใน page.html ก็ได้)
**พื้นฐานที่ได้ใช้:** ไม่มี — จึง **ไม่นับเป็นหน้า 1–3** ใช้เป็นหน้า 4 (page4) ได้

**ปรับให้เป็นของกลุ่ม:** แก้ข้อความใน `sections` หรือลบ loop แล้วเขียน HTML เองทั้งหน้า

**อยากให้หน้า "กติกา / เกี่ยวกับ" นับเป็นหน้า 1–3?** ให้ `build()` คำนวณอะไรจาก data.json จริง ๆ แล้วแสดงในหน้า เช่น
"สถิติที่ต้องทำลายตอนนี้คือ …" (loop หา max), "มีผู้เล่นระดับ X กี่คน" (loop + if), ตารางระดับความเร็วจาก `for level in range(1, 6)` —
แล้ว `check_project.py` จะไม่เตือน
